import styled from "styled-components"
import { fontSizes } from "../../../pages/styles/theme"

export const TitlePage = styled.h2`
  font-size: ${fontSizes.largeTitle * 1.5}px;
  margin: 10px 0px;
`
